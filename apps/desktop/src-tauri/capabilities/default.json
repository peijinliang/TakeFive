{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and reminder windows",
  "windows": ["main", "reminder"],
  "permissions": [
    "core:default",
    "notification:default",
    "process:allow-restart",
    "updater:default"
  ]
}
